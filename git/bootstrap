# shellcheck shell=bash

symlink "$topic/gitconfig"
symlink "$topic/gitconfig.biontech"
symlink "$topic/gitconfig.private"

if [[ "$OSTYPE" == darwin* ]]; then
  formula git
  formula diff-so-fancy
fi


# vim: set ft=sh ts=2 sw=2 et: